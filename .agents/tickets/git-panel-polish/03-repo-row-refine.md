# 03 — repo 行细化（refresh 位置 + 已暂存区在上）

**What to build:** repo 行 refresh 按钮移到 ↓↑ 数字左侧且图标调小（size-3，不再 24px 默认）；展开区"已暂存"区移到"未暂存"区上方（vscode 顺序）。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] refresh 按钮位于 ↓↑ 数字左侧、尺寸小（size-3）
- [ ] 展开区渲染顺序：已暂存在上、未暂存在下
- [ ] npm test + typecheck 全绿
