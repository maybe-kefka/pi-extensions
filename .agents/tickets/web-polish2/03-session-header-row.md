# 03 — 会话标题水平布局

**What to build:** SessionPanel 的"会话"标题 + 刷新按钮水平分布（纯 div flex-row justify-between，不用 CardHeader 默认列布局）。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 标题行 flex-row（jsdom className 断言）
- [ ] 冒烟：会话侧边栏标题/刷新水平 ✓
