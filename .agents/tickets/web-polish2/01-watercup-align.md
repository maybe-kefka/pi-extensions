# 01 — 水杯与 input 同高调宽

**What to build:** chat input 左侧水杯与输入框同一区块且同高（容器 items-center 对齐，高度 = input 高度），宽度 w-3.5（14px）；水位分级逻辑不变。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] WaterCup 宽度 w-3.5、容器与 InputBar 同高同块
- [ ] WaterCup 组件测试更新（宽度类/高度）
- [ ] 冒烟：chat tab 水杯与 input 同高 ✓
