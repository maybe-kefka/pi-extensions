# 04 — split 重挂载后恢复滚动位置

**What to build:** 聊天 tab 因 split 跨父重挂载时，恢复重挂前的滚动位置（不跳到底部）：卸载时上报当前可见消息锚点（可见消息列表首条 id），重挂载且消息渲染就绪后按锚点滚动恢复；锚点消息已不存在则保持默认贴底。锚点存于 App 级 ref（与既有聊天状态快照同模式），不进入聊天流领域状态。

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] 聊天页卸载时上报可见消息锚点；重挂载时按锚点恢复滚动（契约级单测：上报/恢复回调路径）
- [ ] 锚点失效（消息不存在）→ 保持贴底，不报错
- [ ] 浏览器冒烟：滚到历史某处 → split 该 tab → 滚动位置保留
- [ ] 全量测试绿：npm test + typecheck
