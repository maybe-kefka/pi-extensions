# 01 — chip 行内样式

**What to build:** skill/file chip 改为"圆角 bg 的 text"样式——高度与正文行高一致（去除垂直 padding 与 flex 撑高），保留圆角/背景色/图标，字号 12px 保持。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 用户气泡 chip 类名调整（去 py-0.5/行高撑高；rounded + px + align-middle）
- [ ] 输入框/提及相关 chip 若同款一并统一（如有）
- [ ] 组件测试/快照不回归；npm test + typecheck 全绿
