# 04 — 文件操作（新建/重命名/删除）+ git 状态标记

**What to build:** 文件树支持：新建文件/文件夹（弹窗输入名）、重命名（树内联输入框，Enter 确认/Esc 取消）、删除（确认弹窗，递归删除显示项数）；键盘导航（↑↓ 移动/Enter 打开展开/Delete 删除/F2 重命名）；悬停操作按钮。文件树条目显示 git 状态后缀标记（M/A/D/??，目录聚合，颜色随主题）。服务端：文件安全域扩展（mkdir/rename/delete 纯函数 + 路径白名单 + 递归删除项数），新增 `pi:gitStatus`（porcelain 解析 + 目录聚合）。

**Blocked by:** 03 — activity bar 布局重构（四面板 + 聊天全宽）

**Status:** ready-for-agent

- [ ] 新建/重命名/删除真实落盘（浏览器操作验证磁盘）；删除确认含递归项数；非法名/越权被拒有提示
- [ ] 键盘导航与悬停操作可用；树条目 git 状态标记与目录聚合正确
- [ ] Seam A 扩展单测（白名单/递归项数/错误码）+ porcelain 解析/聚合单测；文件操作组件测试
- [ ] `npm test` + `npm run typecheck` 全绿；浏览器冒烟：新建→重命名→删除 + 状态标记实时刷新

