{
  "name": "@kefka/pi-voice",
  "version": "0.1.0",
  "description": "Pi extension: /voice — offline Chinese speech-to-text via local whisper.cpp",
  "type": "module",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maybe-kefka/pi-extensions.git"
  },
  "files": [
    "src",
    "index.ts"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prepublishOnly": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.83.0"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
