{
  "name": "@kefka/pi-notify-termux",
  "version": "0.1.2",
  "description": "Pi extension: Android notification delivery via Termux — agent result notifications with direct-reply input, and LLM ask tools (notify_ask_options / notify_ask_input)",
  "type": "module",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maybe-kefka/pi-extensions.git"
  },
  "files": [
    "src",
    "index.ts"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "prepublishOnly": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "typebox": "^1.3.11"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
